---
name: unterminated-skill
description: the frontmatter fence below is never closed

# Unterminated Frontmatter

The opening `---` has no matching closing fence, so the frontmatter block is
unparseable — a conform/v1 parse-error fixture.
